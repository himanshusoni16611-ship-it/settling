{
"installCommand":"npm install",
    "version": 2,
  "builds": [
    { "src": "server/app.js", "use": "/node" },
    { "src": "build/**", "use": "/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server/app.js" },
    { "src": "/(.*)", "dest": "/build/index.html" }
  ]
}
